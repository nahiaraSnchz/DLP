/*
Este ejemplo tiene varios errores semánticos que el compilador debe detectar en la
fase de identificación y otros que no deben ser detectados aún.

NOTA: Es posible que el alumno deba adaptar ligeramente la sintaxis de este ejemplo si no coincide
con la del lenguaje que le corresponde.
*/

var a:int;

var b:char;

var d:float;

var e:float;

var a:[20] int;	// ERROR: Variable global repetida

// -------------
// ERROR: Parámetro 'z' repetido
// OJO: No hay error en el parámetro 'b', ya que no colisiona con la global del mismo nombre
dobleSuma(z:int, b:int, z:float): int {
	var local1:int;
	var local2:int;
	var local1:int;		// ERROR: Local repetida (con otra local)
	var b:float;		// ERROR: Local repetida (con un parámetro)
	var d:int;		// Esta no debía dar error, ya que la otra 'd' es global

	print a;
	print d;
	print b;
	print c;		// ERROR: variable no definida
	print local2;
}

prueba() {
	print z;	// ERROR: es parámetro de la función anterior
	print local2;	// ERROR: es local de la función anterior
}


main() {
	dobleSuma(5.6);	// No es error en esta fase aunque no coincidan parámetros
}
